version: 5
defaults:
  datadir: /home/antonio/puppet_modules/hieradata/
  data_hash: yaml_data
hierarchy:
  - name: "Per-node data (yaml version)"
    paths:
      - "nodes/%{::fqdn}.yaml"
      - "common.yaml"
