version: 5
defaults:
  datadir: /etc/puppetlabs/code/environments/production/hieradata/
  data_hash: yaml_data
hierarchy:
  - name: "Per-node data (yaml version)"
    paths:
      - "nodes/%{::fqdn}.yaml"
      - "common.yaml"
